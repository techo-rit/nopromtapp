{
  "name": "nopromtapp",
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build && node scripts/copy-web-to-server.mjs"
  }
}
